# Agent Version Manager (AVM)

智能体版本管理器 - 管理 Claude Code、Hermes、Codex 的版本工作流

## 安装

```bash
pip install -e .
```

## 使用

```bash
avm doctor
avm init-project
avm status
```

## 开发

```bash
pip install -e ".[dev]"
pytest
```
